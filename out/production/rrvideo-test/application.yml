spring:
  datasource:
    username: root
    password: root
    url: jdbc:mysql://192.168.126.128:3306/rrvideo_pms
    driver-class-name: com.mysql.cj.jdbc.Driver
mybatis-plus:
  # 扫描当前项目 + 导入的包路径下, resources/mapper下所有文件夹下的.xml文件
  mapper-locations: classpath*:/mapper/**/*.xml

  # 扫描当前项目路径下, resources/mapper下所有文件夹下的.xml文件
  # mapper-locations: classpath:/mapper/**/*.xml


  global-config:
    db-config:
      id-type: auto

